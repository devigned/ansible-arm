# Prototype Ansible Plugin 
Azure Resource Manager template deployment for ansible prototype.

## The ideas from this repo are now upstream. See https://docs.ansible.com/ansible/azure_deployment_module.html
